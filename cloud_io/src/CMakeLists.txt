FIND_PACKAGE(PCL REQUIRED)

#INCLUDE
INCLUDE_DIRECTORIES(${PCL_INCLUDE_DIRS})

#LINK
LINK_DIRECTORIES(${PCL_LIBRARY_DIRS} E://Tools//PCL 1.12.0//3rdParty//VTK//bin)

ADD_EXECUTABLE(${PROJECT_NAME} pcd_io.cpp)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${PCL_LIBRARIES})